#!/usr/bin/env bash

# Temporal enter command

temporal:enter() {
    local service_name="$1"
    
    if ! validate_service_name "$service_name" "enter"; then
        return 1
    fi

    docker exec -it "temporal-server-$service_name" sh
}
