#!/usr/bin/env bash

# Temporal connect command

temporal:connect() {
    local service_name="$1"
    
    if ! validate_service_name "$service_name" "connect"; then
        return 1
    fi

    docker exec -it "temporal-server-$service_name" temporal-cli
}
